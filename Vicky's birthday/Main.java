#include<iostream>
int main()
{
  int a;
  std::cin>>a;
  if(a<1996)
  {
    std::cout<<"Vicky can't celebrate.";
  }
    else if(a==1996)
    {
      std::cout<< "Vicky can celebrate his birthday.";
  }
  else
    std::cout<<"Vicky can celebrate his birthday.";
}